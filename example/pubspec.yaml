name: example_timeline_tile
description: A new Flutter project.

publish_to: 'none'

version: 3.0.1+1

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_highlight: ^0.7.0
  google_fonts: ^6.2.1
  timeline_tile:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/